{
  "name": "agent-lint",
  "version": "0.1.0",
  "description": "CLI that lints stale, broken, and contradictory AI coding instructions in your repository.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-lint": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "clean": "node -e \"import('node:fs/promises').then(fs => fs.rm('dist', { recursive: true, force: true }))\"",
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "prepack": "npm run clean",
    "start": "node src/cli.ts",
    "scan": "node src/cli.ts",
    "test": "node --test tests/unit/*.test.ts tests/integration/*.test.ts",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.8.3"
  }
}
